# 功能参数演示版验收记录（2026-09-04）

## 验收范围

- 本轮覆盖除校方 MySQL 接入、校方真实业务数据之外的功能性参数。
- 演示成绩、学生、班级、规则与实验室图片均明确标记为模拟或演示数据。
- 验收角色：学生、教师、管理员；覆盖桌面端与移动端。

## 自动化验证

- 后端：44 项测试全部通过，覆盖角色权限、评分重算、能力映射、成绩证据链、异步报告任务、环境检查复核与 Mock 同步。
- 前端：3 项角色访问测试全部通过。
- 生产构建：TypeScript 校验与 Vite 生产构建通过。
- 代码检查：`git diff --check` 通过。

## 本地 50 并发验证

- 持续时间：15 秒。
- 请求量：1,784 次；成功率 100%。
- 常规页面接口：平均 0.300 秒，P95 0.801 秒，最大 0.905 秒。
- 汇总统计接口：平均 0.601 秒，P95 0.736 秒，最大 0.859 秒。
- 结果：满足演示环境中常规页面 P95 不高于 3 秒、汇总统计页面 P95 不高于 10 秒的目标。

## 视觉与交互复核

已逐页复核学生学习概览、成绩证据、能力图谱、诊断报告；教师班级概览、成绩下钻、批量报告、环境检查；管理员规则配置、能力映射、重算对比、教师班级授权和同步记录。桌面端与移动端均无页面级横向溢出。

- 信息层级：9.0/10。10 分标准为首次进入即可在一个视觉焦点内确认角色、当前任务与主操作。
- 任务清晰度：8.8/10。10 分标准为所有关键流程均无需解释即可完成，并对每个异步状态给出明确反馈。
- 组件一致性：9.0/10。10 分标准为全站按钮、表单、状态、指标卡与明细证据采用同一套语义和视觉规则。
- 响应式适配：8.4/10。10 分标准为所有目标设备宽度均达到完全等价的信息密度与操作效率。
- 状态与可访问性：8.3/10。10 分标准为键盘、读屏、对比度、错误恢复和加载反馈均经过专项无障碍审计。
- 信息密度与可扫读性：8.2/10。10 分标准为复杂配置页在不减少信息的前提下仍可一次定位任意字段。
- 反模板化质量：9.0/10。10 分标准为不存在无意义渐变、装饰性文案、伪数据暗示或常见生成式界面套壳痕迹。

视觉复核中发现并修复了表单样式缺失、成绩证据弹窗字段溢出、能力图谱首屏动画截帧以及环境标准图缺失问题。三张环境标准图通过内置图片生成能力制作，并作为演示参考素材纳入项目；页面不将其表述为校方真实图片。

## 正式验收边界

以上性能结果用于当前 Demo 工程质量证明，不替代校方服务器、网络、真实数据量和双方确认测试脚本下的正式验收。
